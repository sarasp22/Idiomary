<div class="home-container">
  <div class="home-header-section">
    <div class="language-dropdown">
      <div class="flag-indicator" onclick="document.getElementById('lang-menu').classList.toggle('show')">
        <%= I18n.locale == :en ? "🇬🇧" : (I18n.locale == :it ? "🇮🇹" : (I18n.locale == :fr ? "🇫🇷" : (I18n.locale == :es ? "🇪🇸" : "🇵🇹"))) %>
        <span class="arrow-down">▼</span>
      </div>

      <div id="lang-menu" class="dropdown-content">
        <%= link_to "🇬🇧 English", url_for(locale: :en) unless I18n.locale == :en %>
        <%= link_to "🇮🇹 Italiano", url_for(locale: :it) unless I18n.locale == :it %>
        <%= link_to "🇫🇷 Français", url_for(locale: :fr) unless I18n.locale == :fr %>
        <%= link_to "🇪🇸 Español", url_for(locale: :es) unless I18n.locale == :es %>
        <%= link_to "🇵🇹 Português", url_for(locale: :pt) unless I18n.locale == :pt %>
      </div>
    </div>

    <%= link_to root_path do %>
      <%= image_tag "logo.png", alt: "idiomary logo", class: "home-logo" %>
    <% end %>

    <h1 class="home-tagline"><%= t('site.tagline') %></h1>
    <p class="home-subtitle">
      <%= t('home.languages_desc') %>
    </p>
  </div>

  <div class="home-map-section">
    <div class="map-overlay-content">

      <div class="language-cards">
        <div class="language-card">
          <div class="card-icon-box"><%= image_tag "earth.png", alt: "world icon" %></div>
          <div class="card-text">
            <p class="card-placeholder-text"><%= t('home.card_1.title') %></p>
            <p class="card-sub-placeholder"><%= t('home.card_1.subtitle') %></p>
          </div>
        </div>

        <div class="language-card">
          <div class="card-icon-box"><%= image_tag "glass.png", alt: "world icon" %></div>
          <div class="card-text">
            <p class="card-placeholder-text"><%= t('home.card_2.title') %></p>
            <p class="card-sub-placeholder"><%= t('home.card_2.subtitle') %></p>
          </div>
        </div>
      </div>

      <div class="home-actions">
        <%= link_to t('home.start_searching'), search_path, class: "btn-primary-large" %>

        <% unless user_signed_in? %>
          <div class="auth-links">
            <p class="login-link">
              <%= t('home.already_have_account') %>
              <%= link_to t('nav.login'), new_user_session_path, class: "link-accent" %>
            </p>
            <p class="login-link small-link">
              <%= t('home.new_here') %>
              <%= link_to t('nav.sign_up'), new_user_registration_path, class: "link-accent" %>
            </p>
          </div>
        <% end %>
      </div>

    </div>
  </div>
</div>
