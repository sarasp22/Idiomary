<div class="auth-page-container">
  <div class="auth-language-dropdown">
    <div class="flag-indicator" onclick="document.getElementById('auth-lang-menu').classList.toggle('show')">
      <%= I18n.locale == :en ? "🇬🇧" : (I18n.locale == :it ? "🇮🇹" : (I18n.locale == :fr ? "🇫🇷" : (I18n.locale == :es ? "🇪🇸" : "🇵🇹"))) %>
      <span class="arrow-down">▼</span>
    </div>

    <div id="auth-lang-menu" class="dropdown-content">
      <%= link_to "🇬🇧 English", url_for(locale: :en) unless I18n.locale == :en %>
      <%= link_to "🇮🇹 Italiano", url_for(locale: :it) unless I18n.locale == :it %>
      <%= link_to "🇫🇷 Français", url_for(locale: :fr) unless I18n.locale == :fr %>
      <%= link_to "🇪🇸 Español", url_for(locale: :es) unless I18n.locale == :es %>
      <%= link_to "🇵🇹 Português", url_for(locale: :pt) unless I18n.locale == :pt %>
    </div>
  </div>

  <div class="auth-content-wrapper">
    <div class="auth-logo-section">
      <%= link_to root_path do %>
        <%= image_tag "logo.png", alt: "idiomary logo", class: "auth-logo" %>
      <% end %>
    </div>

    <div class="auth-card">
      <h2 class="auth-title"><%= t('devise.passwords.new.title', default: 'Forgot password?') %></h2>
      <p class="auth-subtitle"><%= t('devise.passwords.new.subtitle', default: 'Enter your email and we\'ll send you a reset link.') %></p>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'auth-form' }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="auth-field">
          <%= f.label :email, class: 'auth-label' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'auth-input', placeholder: 'your@email.com' %>
        </div>

        <div class="auth-actions">
          <%= f.submit t('devise.passwords.new.submit', default: 'Send reset link'), class: 'btn-auth-primary' %>
        </div>
      <% end %>

      <div class="auth-footer-links">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
